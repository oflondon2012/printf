#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdarg.h>

int _myputchar(char c);
int putstr(char *ch);
int _recusive(int num1, int numb2);

int _printf(const char *format, ...);

#endif /* MAIN_H */
